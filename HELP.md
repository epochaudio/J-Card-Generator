# 磁带封面生成器 (J-Card Genesis) 使用帮助

欢迎使用磁带封面生成器。本工具专为实体音乐爱好者设计，帮助你从曲目整理、信息补全、封面生成到导出打印，快速完成一张可直接落地的 J-Card / U-Card。

## 1. 快速开始

### 获取 API Key (必填)
本应用使用阿里云 DashScope (通义千问 & 通义万相) 提供 AI 服务。
1.  访问 [阿里云百炼控制台](https://bailian.console.aliyun.com/?apiKey=1&tab=api#/api)。
2.  登录并开通服务。
3.  复制你的 API Key（以 `sk-` 开头）。
4.  在软件右上角点击 **设置 (齿轮图标)**，将 Key 粘贴到输入框中并点击“完成”。

### 基础流程
1.  **导入或填写专辑信息**:
    *   **文本导入（推荐）**: 使用 `粘贴文本` 导入网易云、Spotify、MusicBrainz、Discogs 或你自行整理的曲目列表，AI 会自动识别标题、艺术家、时长等信息。
    *   **MusicBrainz 搜索**: 点击 `MusicBrainz 搜索`，输入专辑名查找并自动填充信息。
    *   **手动补充**: 可直接修改专辑标题、艺术家、发行日期、目录编号、脊部备注、录音设备、音源、录制日期等字段。
    *   **非古典拖拽整理**: 在 `标准 / 合辑` 模式下，可以直接拖拽曲目卡片左侧把手，进行同面重排或在 `SIDE A / SIDE B` 之间移动。

    > **⚠️ 注意事项**
    > *   如果你准备大幅调整曲序，优先建议先整理完整文本，再通过 `粘贴文本` 一次性导入。
    > *   `古典` 模式的背面是按作品层级自动排版的，目前不支持拖拽排序。

2.  **使用 AI 完成创意补全**:
    *   点击右上角的 **AI 创意总监**，自动补全标题、配色、听感备注等基础信息。
    *   点击 **封面标语** 输入框右侧的 `✨` 按钮，可生成 1-3 句短诗式文案。
    *   点击 **AI 图片提示词** 旁的 **生成提示词**，AI 会根据曲风自动生成更适合插画出图的提示词。

3.  **生成或上传封面**:
    *   点击 **生成封面**，AI 会根据当前提示词生成艺术封面，通常需要 30-60 秒。
    *   也可以点击 **上传图片** 使用本地封面。系统会自动尽量完整显示图片，并用模糊背景填充空白区域。

4.  **调整排版策略**:
    *   在 `标准 / 古典 / 合辑` 之间切换布局模式。
    *   使用 **Short Back 模式** 控制最左侧折页显示 `自动 / 档案信息 / 紧凑曲目`。
    *   通过 **脊部短标题**、`极简脊部`、`强制大写`、`翻转脊部` 等选项微调最终视觉。

5.  **导出打印**:
    *   点击右上角的 **导出 SVG** 或 **导出 PNG**。
    *   `SVG` 适合矢量打印和后期排版，`PNG` 更适合直接预览、分享和照片打印机输出。

## 2. 功能详解 (Feature Guide)

### 2.1 顶部工具栏 (Toolbar)
*   **新建/重置 (Reset)**: 点击顶部的 `旋转箭头` 图标，可以一键清空当前专辑信息，但会保留你的 API Key、录音设备、音源设置和主题偏好。
*   **主题切换 (Themes)**: 顶部支持 `[ Dark | Light | Cover | Color ]` 四种视觉主题，J-Card 预览会即时切换。
*   **字体风格 (Fonts)**: 支持 `Moderno (现代)`、`Analog (复古)`、`Handwritten (手写)`、`Digital (数码)` 四种字体系统，预览与导出保持一致。

### 2.2 核心输入区 (Input Panel)
*   **布局模式 (Layout Mode)**:
    *   `标准 (Standard)`: 适合大多数流行、摇滚、电子、独立专辑。
    *   `古典 (Classical)`: 自动识别作品与乐章层级，背面会优先展示结构化曲目信息与档案元数据。
    *   `合辑 (Compilation)`: 更强调单曲艺术家显示，适合拼盘、群星、精选集。
*   **脊部备注 (Spine Notes)**:
    *   `Note Upper`: 显示在脊部最上方，常填写 `STEREO / MONO`、年份、录音日期等。
    *   `Note Lower`: 显示在脊部最下方，常填写版权、版本信息或自定义说明。
*   **脊部短标题 (Spine Short Title)**:
    *   当完整标题过长时，系统会优先尝试完整标题单行、双行，再视情况切换到手动短标题、自动短标题或截短兜底。
    *   留空时系统会自动生成短标题；手动填写时，可更稳定地控制最终脊部文案。
*   **档案元数据 (Archival Metadata)**:
    *   **Recording Equipment**: 记录卡座、线材、解码链路等设备信息，支持多行文本并自动保存。
    *   **Media Source**: 记录音源来源，如 `Vinyl`、`SACD`、`Streaming`。
    *   **Tape Rec. Date**: 记录转录磁带的日期，默认当天，可手动修改。

### 2.3 封面与样式 (Cover & Style)
*   **上传图片**: 支持直接上传本地图片作为封面，推荐使用接近 1:1 的正方形图，但非正方形图片也可正常使用。
*   **AI 提示词生成**: `AI 图片提示词` 支持独立生成，可先生成、再手动微调后出图。
*   **AI 封面生成**: 使用通义万相生成高清图片，适合没有现成封面图或希望做二创封面的场景。
*   **零裁剪 / Full Fit**: 前封面采用“尽量完整展示主体 + 模糊背景补边”的策略，降低非正方形图片被硬裁切的风险。

### 2.4 曲目管理 (Tracklist)
*   **智能导入 (Smart Import)**: 推荐优先使用 `粘贴文本`，对来自不同平台的曲目文本容错更高，也更适合批量整理。
*   **手动编辑**: 可以逐条修改 `标题 / 时长 / 艺术家 / 备注`，也可以单独删除、补充曲目。
*   **拖拽排序**:
    *   `标准 / 合辑` 模式下，曲目卡片左侧会出现拖拽把手。
    *   支持同侧重排，也支持在 `SIDE A` 与 `SIDE B` 间跨面移动。
    *   拖拽后会自动重算两面总时长，并立即刷新预览与背面内容。
*   **稳定 Track ID**: 每条曲目都有独立稳定 ID，重排后输入框不会错位，编辑体验更稳。

### 2.5 背面与折页策略 (Back & Short Back)
*   **Back 面板密度控制**:
    *   `古典` 模式下，系统会根据实际可用空间在 `完整分曲 -> 节选分曲 -> 作品摘要 -> 仅作品名` 之间逐级降级，避免背面溢出。
    *   `标准 / 合辑` 模式下，系统会优先保留 `标题 + 备注`，再按密度逐步隐藏 `artist / duration`，避免信息全线压坏。
*   **真实测量驱动**: 这些降级决策基于真实字体测量和可用高度计算，而不是简单按条目数硬裁剪。
*   **Short Back 独立模式**:
    *   `自动`: 根据当前布局模式自动选择更合适的左侧折页内容。
    *   `档案信息`: 更偏向展示 `label / source / credits / equipment / release / recorded` 等信息。
    *   `紧凑曲目`: 更偏向展示折叠后的曲目摘要。
*   **古典 Short Back 改进**: 古典 `Short Back (Fold-in)` 的档案模式采用独立的先测量后渲染布局流程，长字段更不容易贴边、越界或被硬裁切。

## 3. 常见问题 (FAQ)

**Q: 为什么生成图片很慢？**
A: 使用的高清图像模型（Wanx 2.5）需要较多计算资源，通常需要 30-60 秒。请耐心等待。

**Q: 生成的图片不满意怎么办？**
A: 先修改 `AI 图片提示词`，补充更具体的画面、媒介或情绪描述，再重新点击 `生成封面`。如果只是文字气质不对，也可以单独重新生成 `封面标语`。

**Q: 为什么脊部标题被缩得很小，或者显示成短标题？**
A: 这是脊部保护策略在生效。系统会优先尝试完整标题，再根据空间自动切换到双行、短标题或截短兜底。你可以手动填写 **脊部短标题** 来稳定控制最终显示。

**Q: 为什么背面有时不显示艺术家或时长？**
A: 这是背面密度保护策略。空间不足时，系统会优先保留更关键的信息。`标准 / 合辑` 模式通常优先保留 `标题 + 备注`，再逐步收起 `artist / duration`，以避免整个背面挤坏。

**Q: MusicBrainz 搜索不到图片，或曲目时间不对？**
A:
*   **图片缺失**: 可能是封面存档服务器连接不稳，或该条目本身没有上传封面。
*   **时间数据**: 曲目时长直接取自 MusicBrainz 数据库，A/B 面分割会根据总时长自动计算。
*   **补救办法**: 建议从 Discogs、Amazon、Bandcamp 或其他页面复制带时长的曲目文本，再通过 **粘贴文本** 导入。

**Q: 如何打印？**
A: 推荐导出 PNG 格式，使用 Adobe Illustrator、浏览器或 Canon SELPHY Photo Layout 打印。标准 J-Card 尺寸已预设好。**如果使用 Canon SELPHY CP1500 / SELPHY Photo Layout，请优先选择“适配尺寸”，不要选择“充满”**，否则打印端可能会二次放大并裁掉边缘，导致脊部、折线和外边距失准。

## 4. 打印与规格说明

### 推荐打印设备：Canon SELPHY CP1500
本工具生成的图片比例完美适配佳能炫飞 (SELPHY) 系列打印机的 6 寸相纸。
*   **相纸规格**: 6 寸 (4R / 4x6 英寸)
*   **实际尺寸 (无边距)**: 约 **100 x 148 mm**
*   **关键设置**: 在 `SELPHY Photo Layout` 或相关打印界面中，**请选择“适配尺寸”**。
*   **不要选“充满”**: `充满` 会把 J-Card 当作普通照片再次放大，容易裁掉四周边缘，导致封底、脊部或折页留位偏掉。
*   **灰色留空是正常的**: 预览界面里看到的灰色区域通常只是 App 的工作区背景，不代表最终会多打印出一圈白边。判断标准应以 J-Card 四周是否完整保留为准。
*   **使用建议**: 导出 PNG 后，直接使用手机或电脑连接 CP1500 打印即可，无需手动裁剪；保持 `适配尺寸`、不要额外放大，即打即用。

### J-Card 布局说明 (四折页 / U-Card 风格)
本生成器采用的是扩展型的 J-Card 布局（也称 U-Card 风格），包含以下部分：
1.  **封面 (Front)**: 磁带盒正面的正方形区域。
2.  **脊部 (Spine)**: 侧面的长条区域，显示专辑名和编号。
3.  **封底 (Back)**: 脊部另一侧的折叠页，通常包含 A 面曲目。
4.  **扩展页 (Extension)**: 额外的折叠面板，用于容纳 B 面曲目、详细制作人员名单或歌词。
*   **总展开效果**: 打印并折叠后，可以完美包裹磁带盒，不仅提供更多信息展示空间，也比普通 J-Card 更具收藏质感。

## 5. 版权与许可声明 (Copyright & License)

**© 2025 门耳朵 (Epoch Audio). 保留所有权利。**

本软件为开源软件，允许个人在**非商业目的**下免费使用、学习与研究。

### 🚫 禁止商业用途
**禁止任何形式的商业化用途**，包括但不限于：
1.  售卖软件本体、安装包或激活服务。
2.  以本软件提供有偿服务（如有偿打印、代处理数据、代制作封面）。
3.  付费下载、会员专享、积分兑换等变相收费。
4.  植入广告、引流、分成等变现行为。
5.  用于企业、网店或实体门店的经营场景。

### ✅ 关于转载与二次开发
允许在**非商业目的**下转载、镜像、分享、二次开发或再发布，但必须遵守以下条件：
1.  **完整保留**本声明与原作者署名。
2.  **不得收费**或设置任何付费门槛（如关注后下载）。
3.  不得暗示获得原作者的背书或授权。
4.  不得植入任何广告、恶意代码或追踪程序。

*本软件按“现状”提供，作者不对使用本软件产生的任何后果承担责任。商业授权请另行取得书面许可。*
